command_contract_id: grill.v2
stable_alias: /grill
contract_version: 2

input_artifacts: []

output_artifacts:
  - type: GrillRecord
    schema: grill.schema.v2
    required: true
  - type: TaskManifest
    schema: task-manifest.schema.v1
    required: true

required_validators:
  after:
    - validate-artifact
    - validate-artifact-path

authority:
  may_create:
    - GrillRecord
    - TaskManifest
  may_modify: []
  may_read:
    - task_scope_files_only
  forbidden:
    - edit_source_files
    - modify_manifest
    - modify_schemas
    - modify_policies
    - scan_whole_repo
    - bypass_validation

semantics:
  required:
    - ask_one_question_at_a_time
    - provide_recommended_answer_per_question
    - capture_approach_decisions_with_alternatives_rejected
    - record_decision_proceed_or_stop
    - save_grill_record_to_task_folder
    - create_task_manifest_at_task_birth
    - produce_structured_success_criteria_objects
    - each_criterion_must_have_stable_id_and_verification_type
    - free_text_criteria_not_accepted
