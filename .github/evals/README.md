# Evaluation Runs

`/evaluate` writes dashboard-ready metrics under `.github/evals/runs/EVAL-YYYYMMDD-HHMM/`.

Evaluation output is local analysis data, not workflow memory and not a dashboard UI. Generated run directories are gitignored.
