# /upgrade-workflow

Scan all task artifacts for schema mismatches after a bundle upgrade. Reports compatible, migration_required, or regenerate status per artifact.

#file:.github/agents/upgrade-workflow.agent.md
