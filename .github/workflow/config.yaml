# Written by /setup-workflow — do not edit manually.
# Run /setup-workflow to populate or update these values.

project:
  name: "Office"
  description: "Workflow tooling for GitHub Copilot (IntelliJ) and Claude Code."
  primary_language: "Markdown"
  framework: "none"

commands:
  build: "none"
  test: "none"
  verify: "python3 .github/ai-workflow/validators/bootstrap && python3 .github/ai-workflow/validators/validate-manifest && python3 .github/ai-workflow/validators/validate-config .github/workflow/config.yaml"
  lint: "none"

workflow:
  cli_handoff_allowed: true
  task_path: .github/tasks
  instruction_version: v1
